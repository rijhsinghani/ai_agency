---
phase: 13-landing-page-rebuild-sameerautomations
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - website-v2/app/page.tsx
  - website-v2/components/sections/nav.tsx
  - website-v2/components/sections/hero.tsx
  - website-v2/components/sections/input-output-bridge.tsx
  - website-v2/components/sections/industry-proof-strip.tsx
  - website-v2/components/sections/problem-strip.tsx
  - website-v2/components/sections/how-it-works.tsx
autonomous: true
requirements: [LP-02, LP-03]
must_haves:
  truths:
    - "Sticky nav renders logo, Work, How it works, and Book a call CTA"
    - "Hero shows brand headline, subhead, two CTAs, and animated Input/Output Bridge visual"
    - "Industry proof strip names ≥5 distinct non-photo industries under the hero"
    - "Problem strip renders 3 pain tiles with peer-level voice"
    - "How-it-works section renders 4 steps: discovery, scope, build, handoff + retainer"
    - "All copy is lint-clean against brand/banned-phrases.json"
  artifacts:
    - path: "website-v2/components/sections/hero.tsx"
      provides: "Hero section with headline/CTAs/Bridge visual"
    - path: "website-v2/components/sections/input-output-bridge.tsx"
      provides: "Animated S-curve SVG with purple→cyan gradient + Framer Motion"
    - path: "website-v2/components/sections/how-it-works.tsx"
      provides: "4-step process with retainer transparency"
  key_links:
    - from: "website-v2/app/page.tsx"
      to: "website-v2/components/sections/*"
      via: "direct imports of Nav/Hero/ProblemStrip/HowItWorks"
    - from: "hero.tsx CTA"
      to: "Google Calendar booking URL (reused from website/index.html)"
      via: "anchor href"
---

<objective>
Wave 2 builder-A plan. Build the top-of-page sections (nav, hero with Bridge visual, problem strip, how-it-works) as production React components using shadcn primitives, Framer Motion, and the style plan from 13-01. Runs in isolated git worktree `~/.claude/worktrees/website-v2-builder-a` to parallelize with builder-B.

Purpose: Deliver the sections that establish first impression and brand voice. Hero + Bridge visual is the signature moment of the page.

Output: 4 section components wired into `app/page.tsx`, each an atomic commit, running in a git worktree for conflict-free parallel work with builder-B.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/automation_consulting/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-CONTEXT.md
@~/.claude/plans/snazzy-sauteeing-pixel.md
@website-v2/research/style-plan.md
@website-v2/research/inspiration.md
@brand/brand-voice.md
@brand/banned-phrases.json
@website/hero-preview.html
@website/hero-spline-preview.html
</context>

<interfaces>
<!-- Contracts Wave 2 builders rely on from 13-02 scaffold -->

From `website-v2/tailwind.config.ts`:

```ts
colors: { base, surface, cream, 'cream-dim', purple, 'purple-soft', cyan, orange }
fontFamily: { display: ['Roc Grotesk',...], body: ['Gilroy',...], mono: ['JetBrains Mono',...] }
```

From `website-v2/app/globals.css`:

```css
.dot-grid {
  /* radial dot pattern 8% opacity */
}
.arch-node {
  /* glowing node helper */
}
.grain {
  /* noise overlay */
}
.gradient-brand {
  /* purple → light purple → cyan */
}
```

shadcn primitives available at `@/components/ui/{button,card,accordion,dialog}`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Set up builder-A worktree + build Nav + Hero + Input/Output Bridge visual</name>
  <files>website-v2/components/sections/nav.tsx, website-v2/components/sections/hero.tsx, website-v2/components/sections/input-output-bridge.tsx, website-v2/components/sections/industry-proof-strip.tsx, website-v2/app/page.tsx</files>
  <action>
    First create the isolated worktree from repo root (use the `superpowers:using-git-worktrees` skill pattern):
      git worktree add ~/.claude/worktrees/website-v2-builder-a -b wave2/builder-a
    All subsequent edits happen in that worktree. `cd ~/.claude/worktrees/website-v2-builder-a/website-v2 && pnpm install`.

    Build components/sections/nav.tsx:
      - Sticky (position fixed/top-0), backdrop-blur, transparent → surface on scroll
      - Logo left (img from /brand/), links center: Work / How it works, CTA right: shadcn Button variant primary → "Book a call" → Google Calendar URL (copy from website/index.html — search for calendar link). Mobile: hamburger Dialog.

    Build components/sections/input-output-bridge.tsx:
      - Pure React SVG, 100x100 viewBox scaled via className
      - S-curve cubic bezier path with 3-stop gradient (stop-color #7B2FBE at 0, #9B4FDE at 0.5, #4DD9E8 at 1)
      - Input node (top-right): purple circle + SVG feGaussianBlur glow + white filament dot
      - Output node (bottom-left): cyan circle with cyan glow + white filament
      - Framer Motion: path strokeDashoffset animates from length→0 over 2.2s ease-out on mount, nodes scale 0.8→1 with opacity, loop subtle breathing at 4s intervals. Respect `prefers-reduced-motion` (bail to static render).
      - Try SVG first per PRD. Do NOT pull in Spline/R3F unless SVG is visibly insufficient.

    Build components/sections/hero.tsx:
      - Two-column grid (lg:grid-cols-2, stack on mobile)
      - Left: eyebrow "Automation for small business owners", H1 (Roc Grotesk, large, tight leading) "Every lead gets a response in 60 seconds.", subhead 2 lines from brand-voice.md (peer-level, direct, confident), two CTAs: primary Button "Book a call" + ghost Button "See what I build" → #how-it-works anchor. Sentence case headings. Oxford commas. No emojis.
      - Right: <InputOutputBridge /> component centered, dot-grid background layer + arch-node glow spots at low opacity
      - Framer Motion fade-up-on-mount for left column, delay stagger 80ms between eyebrow/headline/subhead/CTAs

    Build components/sections/industry-proof-strip.tsx:
      - Thin horizontal strip directly below hero
      - Text: "Built for dental offices, salons, trades, cleaning companies, law firms, gyms, real estate teams, and tutoring centers."
      - Must name ≥5 distinct non-photo industries (this one names 8 — satisfies PRD)
      - Subtle fade-in on scroll

    Replace app/page.tsx to compose: `<Nav /><Hero /><IndustryProofStrip />` — leave placeholders for downstream sections as HTML comments.

    Before committing, grep every string in these files against brand/banned-phrases.json — zero matches required. Commit atomically from worktree:
      git add website-v2/components/sections/nav.tsx website-v2/components/sections/hero.tsx website-v2/components/sections/input-output-bridge.tsx website-v2/components/sections/industry-proof-strip.tsx website-v2/app/page.tsx && git commit -m "feat(13-03): nav + hero + Input/Output Bridge + industry proof strip"

  </action>
  <verify>
    <automated>cd ~/.claude/worktrees/website-v2-builder-a/website-v2 && pnpm exec tsc --noEmit && test -f components/sections/hero.tsx && test -f components/sections/input-output-bridge.tsx && grep -q 'Book a call' components/sections/hero.tsx && node -e "const bp=require('../../../../brand/banned-phrases.json'); const fs=require('fs'); const files=['components/sections/nav.tsx','components/sections/hero.tsx','components/sections/input-output-bridge.tsx','components/sections/industry-proof-strip.tsx']; let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log('HIT',f,p);hits++}}} process.exit(hits?1:0)"</automated>
  </verify>
  <done>Nav, Hero, Bridge visual, and industry strip render; TypeScript compiles; banned-phrases lint is clean; atomic commit landed on wave2/builder-a branch</done>
</task>

<task type="auto">
  <name>Task 2: Build Problem Strip + How It Works sections and wire into page</name>
  <files>website-v2/components/sections/problem-strip.tsx, website-v2/components/sections/how-it-works.tsx, website-v2/app/page.tsx</files>
  <action>
    Continue in `~/.claude/worktrees/website-v2-builder-a/website-v2`.

    Build components/sections/problem-strip.tsx:
      - 3 shadcn Cards in grid (md:grid-cols-3), dark surface, subtle border, no emojis
      - Tile 1: "Leads go cold" — body: "You're on a job. The call goes to voicemail. By the time you're back, they've already hired someone else."
      - Tile 2: "Calls get missed" — body: "40% of inbound calls to small service businesses never get a callback. That's revenue walking out the door."
      - Tile 3: "Manual follow-up eats your week" — body: "Chasing quotes, sending reminders, asking for reviews. Hours a week on work a system should handle."
      - Peer-level voice per brand-voice.md §2. Sentence case. Oxford commas. Numerals for metrics.
      - Framer Motion: fade-up stagger on viewport enter (whileInView, once: true)

    Build components/sections/how-it-works.tsx:
      - H2: "How it works"
      - 4 steps in horizontal flow on desktop (connector lines purple→cyan gradient), vertical on mobile
        1. Discovery call — "We talk for 15 minutes. I figure out where leads are leaking or where your week is getting eaten. No pitch."
        2. Scope — "I send back a plain-English proposal. What I'll build, what it costs, what it'll do for you in numbers."
        3. Build — "Days, not weeks. You get progress updates. I build it custom to your tools and your workflow."
        4. Handoff and retainer — "I hand it over, train you, and stay on the monthly retainer to keep it running. Setup is $1,500–$3,000. Retainer is $200–$500/mo. That's the whole deal."
      - Arch-node glow at each step indicator, dot-grid background at low opacity
      - Explicit price transparency per brand-voice.md §2 Transparent

    Update app/page.tsx to compose: `<Nav /><Hero /><IndustryProofStrip /><ProblemStrip /><HowItWorks />`. Leave comment placeholders where builder-B sections will land so merge is clean.

    Lint-check all new strings against banned-phrases.json (zero matches). Commit atomically:
      git add website-v2/components/sections/problem-strip.tsx website-v2/components/sections/how-it-works.tsx website-v2/app/page.tsx && git commit -m "feat(13-03): problem strip + how it works"

  </action>
  <verify>
    <automated>cd ~/.claude/worktrees/website-v2-builder-a/website-v2 && pnpm exec tsc --noEmit && grep -q '1,500' components/sections/how-it-works.tsx && grep -q 'Discovery call' components/sections/how-it-works.tsx && node -e "const bp=require('../../../../brand/banned-phrases.json'); const fs=require('fs'); const files=['components/sections/problem-strip.tsx','components/sections/how-it-works.tsx']; let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log('HIT',f,p);hits++}}} process.exit(hits?1:0)"</automated>
  </verify>
  <done>Problem strip + how-it-works render with brand voice + price transparency; TypeScript compiles; banned-phrases clean; atomic commit landed</done>
</task>

</tasks>

<success_criteria>
Builder-A's 4 sections (nav, hero+Bridge+strip, problem strip, how-it-works) are implemented, committed atomically on branch `wave2/builder-a`, TypeScript-clean, and lint-clean against banned-phrases.json. Ready for shipper to merge alongside builder-B's branch in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/13-landing-page-rebuild-sameerautomations/13-03-SUMMARY.md` on completion. Note worktree path and branch name for shipper merge.
</output>
