---
phase: 13-landing-page-rebuild-sameerautomations
plan: 06
type: execute
wave: 4
depends_on: [13-05]
files_modified:
  - website-v1-archive/
  - .planning/PROJECT.md
  - .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md
autonomous: false
requirements: [LP-05, LP-06]
must_haves:
  truths:
    - "Lighthouse mobile profile: perf ≥95, a11y 100, SEO 100, best-practices 100"
    - "curl -I https://sameerautomations.com returns 200 HTTPS with HSTS"
    - "Real form submission appears in n8n execution history"
    - "Plausible records a page view and a book-call-click event within 60s"
    - "At least 5 distinct non-photo industries named on the live page"
    - "Legacy website/ is moved to website-v1-archive/ (not deleted)"
    - "DNS is cut over in Cloudflare; old site no longer resolves on sameerautomations.com"
    - "Cutover announced in Slack #claude"
  artifacts:
    - path: "website-v1-archive/"
      provides: "Legacy site preserved for rollback"
    - path: ".planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md"
      provides: "Pass/fail record for every verification gate"
  key_links:
    - from: "Cloudflare DNS"
      to: "Cloud Run domain mapping (from 13-05 SUMMARY)"
      via: "CNAME or A record per gcloud output"
---

<objective>
Wave 4 verification + cutover plan. Runs the full PRD verification block, cuts DNS in Cloudflare, archives the legacy `website/` directory, updates `.planning/PROJECT.md`, and announces go-live in Slack #claude. Contains one checkpoint (DNS cut) because it is a live user-facing action that benefits from a quick human sanity check even though gcloud and Cloudflare CLI/API handle the mechanics.

Purpose: Turn a deployed-but-not-public deployment into the live sameerautomations.com, preserve rollback capacity, and close out the phase.

Output: Live `https://sameerautomations.com`, archived v1 site, completed verification report, announcement posted.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/automation_consulting/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-CONTEXT.md
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-05-SUMMARY.md
@~/.claude/plans/snazzy-sauteeing-pixel.md
@brand/banned-phrases.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local + docker + Cloud Run preflight verification</name>
  <files>.planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md</files>
  <action>
    Start a fresh `.planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md` and record PASS/FAIL for each gate below. Any FAIL blocks progression — fix in a targeted patch and re-run.

    Gate 1 — Local dev smoke test:
      cd website-v2 && pnpm install && pnpm dev & sleep 8 && curl -sf http://localhost:3000 | grep -qi 'sameer'; kill %1

    Gate 2 — TypeScript + build:
      cd website-v2 && pnpm exec tsc --noEmit && pnpm build

    Gate 3 — Copy lint (banned-phrases zero matches):
      node -e "const bp=require('./brand/banned-phrases.json'); const fs=require('fs'); const path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name); return e.isDirectory()?walk(p):[p]})}; const files=walk('website-v2/app').concat(walk('website-v2/components')).filter(f=>/\.(tsx?|jsx?)$/.test(f)); let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log(f,p);hits++}}} if(hits)process.exit(1); console.log('CLEAN')"

    Gate 4 — ≥5 distinct non-photo industries named on the live page:
      curl -sf https://sameerautomations.com 2>/dev/null || curl -sf "$(gcloud run services describe sameer-automations-web --region us-east4 --format='value(status.url)')" > /tmp/live.html
      node -e "const html=require('fs').readFileSync('/tmp/live.html','utf8').toLowerCase(); const inds=['dental','salon','trades','cleaning','law','gym','real estate','tutoring','automotive','roofing']; const hits=inds.filter(i=>html.includes(i)); console.log('industries:',hits); if(hits.length<5)process.exit(1)"

    Gate 5 — Docker build + run locally:
      cd website-v2 && docker build -t saw-verify . && docker run -d -p 8081:8080 -e N8N_LEAD_WEBHOOK_URL=http://example.invalid --name saw-verify saw-verify && sleep 5 && curl -sfI http://localhost:8081 | head -1 | grep -q 200; docker rm -f saw-verify

    Gate 6 — Cloud Run service READY:
      gcloud run services describe sameer-automations-web --region us-east4 --format='value(status.conditions[0].type,status.conditions[0].status)' | grep -q 'Ready.*True'

    Gate 7 — Lighthouse mobile ≥95 perf, 100 a11y/SEO/BP via chrome-devtools-mcp:
      Use chrome-devtools-mcp to run Lighthouse on the Cloud Run service URL (pre-DNS-cut). Record all 4 scores. Any score below threshold is a FAIL.

    Gate 8 — Responsive QA at 375 / 768 / 1440 via chrome-devtools-mcp screenshots. Confirm no horizontal scroll, fonts loaded, dot-grid visible, Bridge visual renders. Attach screenshot paths to 13-VERIFICATION.md.

    Write results for every gate to 13-VERIFICATION.md with `status: PASS|FAIL` and evidence. Commit:
      git add .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md && git commit -m "verify(13-06): preflight gates recorded"

  </action>
  <verify>
    <automated>test -f .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md && grep -c 'PASS' .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md | awk '$1 >= 8 {exit 0} {exit 1}' && ! grep -q 'FAIL' .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md</automated>
  </verify>
  <done>All 8 preflight gates PASS; 13-VERIFICATION.md committed; zero FAIL entries</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Cloud Run service `sameer-automations-web` (us-east4) is deployed with the full v2 site. Domain mapping for `sameerautomations.com` is created (from 13-05) and waiting for DNS. Preflight verification gates (Gates 1–8) all PASSED in Task 1. The next step is to cut Cloudflare DNS, which switches real traffic from the legacy `website/` deployment to the new Cloud Run service.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md` and confirm all 8 gates are PASS.
    2. Visit the Cloud Run service URL directly (from 13-05-SUMMARY.md) in a real browser on mobile and desktop. Click through every section. Click "Book a call" — confirm it opens the Google Calendar booking page. Submit the lead form with real test data — confirm n8n receives it in the execution log and Slack #claude gets a notification.
    3. Open Plausible dashboard — confirm the test visit and the `book-call-click` event appear within 60 seconds.
    4. Confirm you are ready to cut DNS. Once DNS cuts, sameerautomations.com will serve the new site globally (Cloudflare TTL may take a few minutes to propagate).
  </how-to-verify>
  <resume-signal>Type "cut dns" to proceed, or describe issues to fix before cutover.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Cut Cloudflare DNS + verify live HTTPS + fire real events</name>
  <files>.planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md</files>
  <action>
    Read 13-05-SUMMARY.md for the exact DNS record Cloud Run's domain mapping requires (CNAME ghs.googlehosted.com or A records). Apply it via Cloudflare CLI or API:
      # If using flarectl or cloudflare-go CLI:
      flarectl dns create --zone rajphotovideo.com --name sameerautomations.com --type CNAME --content ghs.googlehosted.com --proxy=false
      # OR use curl + CLOUDFLARE_API_TOKEN against https://api.cloudflare.com/client/v4/zones/.../dns_records
      # If DNS is managed in a different zone (sameerautomations.com as its own zone), adjust zone flag accordingly.

    Wait for DNS propagation (up to 5 minutes) and TLS certificate to issue:
      for i in 1 2 3 4 5 6; do curl -sI https://sameerautomations.com 2>/dev/null | head -1 | grep -q 200 && break; sleep 30; done

    Gate 9 — Live HTTPS:
      curl -sI https://sameerautomations.com | tee -a .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md
      # Must show: 200 OK, HSTS header (strict-transport-security), HTML content-type

    Gate 10 — Real form E2E on live domain:
      Submit via curl: curl -sf -X POST https://sameerautomations.com/api/lead -H 'content-type: application/json' -d '{"name":"verify","email":"verify@example.com","business":"verify co","industry":"dental","message":"cutover test"}' | grep -q '"ok":true'
      Then confirm n8n execution log shows the inbound with source='sameerautomations.com'.

    Gate 11 — Plausible live event:
      curl -sf https://sameerautomations.com >/dev/null
      # Wait 60s — check Plausible dashboard (via browser-use or chrome-devtools-mcp) for a fresh page view.

    Append results to 13-VERIFICATION.md. Commit:
      git add .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md && git commit -m "verify(13-06): live domain verified, gates 9-11 PASS"

  </action>
  <verify>
    <automated>curl -sI https://sameerautomations.com 2>/dev/null | head -1 | grep -q 200 && curl -sI https://sameerautomations.com 2>/dev/null | grep -qi 'strict-transport-security' && grep -q 'Gate 9.*PASS' .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md</automated>
  </verify>
  <done>sameerautomations.com serves the new site over HTTPS with HSTS; form E2E confirmed; Plausible event confirmed; Gates 9–11 PASS</done>
</task>

<task type="auto">
  <name>Task 3: Archive legacy website/ + update PROJECT.md + announce cutover</name>
  <files>website-v1-archive/, .planning/PROJECT.md</files>
  <action>
    Archive the legacy site — do NOT delete it:
      git mv website website-v1-archive
      # Confirm nothing in website-v2/ or cloudbuild.yaml references ../website/
      grep -rn "website/" website-v2 cloudbuild.yaml .github/workflows/deploy-website.yml 2>/dev/null | grep -v website-v1-archive || true

    Update .planning/PROJECT.md: set current-phase note to "Phase 13 complete — sameerautomations.com live on Cloud Run us-east4". Do not touch positioning/out-of-scope sections.

    Commit:
      git add website website-v1-archive .planning/PROJECT.md && git commit -m "chore(13-06): archive legacy website -> website-v1-archive + update PROJECT.md"

    Announce in Slack #claude via the existing slack-bridge or openclaw channel (whichever is running):
      # Example via gcloud/curl to the openclaw HTTP surface, or by dropping a note in the session channel so the supervisor posts it.
      Message body: "sameerautomations.com is live. Cloud Run us-east4, sameer-automations-web. Legacy site archived at website-v1-archive/. Verification report at .planning/phases/13-landing-page-rebuild-sameerautomations/13-VERIFICATION.md."

  </action>
  <verify>
    <automated>test -d website-v1-archive && ! test -d website && grep -q 'Phase 13' .planning/PROJECT.md && git log -1 --oneline | grep -q '13-06'</automated>
  </verify>
  <done>Legacy site archived, PROJECT.md updated, cutover announced, phase closed</done>
</task>

</tasks>

<success_criteria>

1. All 11 verification gates PASS in 13-VERIFICATION.md
2. `curl -I https://sameerautomations.com` returns 200 + HSTS
3. Real lead form submission confirmed in n8n
4. Plausible shows live traffic + book-call-click event
5. `website-v1-archive/` exists, `website/` no longer exists
6. PROJECT.md reflects Phase 13 complete; Slack #claude cutover message posted
   </success_criteria>

<output>
Create `.planning/phases/13-landing-page-rebuild-sameerautomations/13-06-SUMMARY.md` on completion with the final live URL, Cloud Run revision, and a link to 13-VERIFICATION.md.
</output>
