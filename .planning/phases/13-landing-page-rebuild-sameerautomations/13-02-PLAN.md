---
phase: 13-landing-page-rebuild-sameerautomations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website-v2/package.json
  - website-v2/tsconfig.json
  - website-v2/next.config.ts
  - website-v2/tailwind.config.ts
  - website-v2/postcss.config.mjs
  - website-v2/app/layout.tsx
  - website-v2/app/page.tsx
  - website-v2/app/globals.css
  - website-v2/components.json
  - website-v2/components/ui/button.tsx
  - website-v2/components/ui/card.tsx
  - website-v2/components/ui/accordion.tsx
  - website-v2/components/ui/dialog.tsx
  - website-v2/public/brand/
  - website-v2/public/fonts/
autonomous: true
requirements: [LP-01]
must_haves:
  truths:
    - "website-v2/ is a runnable Next.js 15 + TypeScript + Tailwind v4 + App Router app"
    - "shadcn/ui is initialized with Button, Card, Accordion, Dialog primitives"
    - "Brand tokens (#7B2FBE, #9B4FDE, #4DD9E8, #1A1A1A) and fonts (Roc Grotesk, Gilroy) are wired in tailwind.config.ts and globals.css"
    - "Brand logo assets copied to website-v2/public/brand/"
    - "Global CSS provides dot-grid utility, arch-node glow helper, grain overlay"
  artifacts:
    - path: "website-v2/tailwind.config.ts"
      provides: "Brand token palette + font family mappings"
    - path: "website-v2/app/globals.css"
      provides: "Font-face, dot-grid util, arch-node glow, grain overlay"
    - path: "website-v2/components/ui/button.tsx"
      provides: "shadcn Button primitive"
  key_links:
    - from: "website-v2/app/layout.tsx"
      to: "website-v2/app/globals.css"
      via: "import './globals.css'"
    - from: "website-v2/tailwind.config.ts"
      to: "brand/brand-voice.md §9 token table"
      via: "manual port of palette + fonts"
---

<objective>
Wave 1 builder-A scaffold plan. Creates a runnable Next.js 15 + Tailwind v4 + shadcn/ui skeleton under `website-v2/` with all brand tokens, fonts, and visual utilities pre-wired so Wave 2 builders can drop section components in without touching config.

Purpose: Give both Wave 2 builders a zero-friction surface. No style decisions, no package installs, no token lookups in Wave 2.

Output: A `website-v2/` directory that runs `pnpm dev` successfully, serves an empty App Router page with brand fonts loaded and dot-grid background visible.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/automation_consulting/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-CONTEXT.md
@~/.claude/plans/snazzy-sauteeing-pixel.md
@brand/brand-voice.md
@website/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 app + core deps</name>
  <files>website-v2/package.json, website-v2/tsconfig.json, website-v2/next.config.ts, website-v2/postcss.config.mjs, website-v2/app/layout.tsx, website-v2/app/page.tsx</files>
  <action>
    From repo root run: `pnpm create next-app@latest website-v2 --typescript --tailwind --app --use-pnpm --no-eslint --src-dir=false --import-alias "@/*"`. Accept defaults for everything else. Then `cd website-v2 && pnpm add framer-motion lucide-react clsx tailwind-merge class-variance-authority && pnpm add -D @types/node eslint eslint-config-next`. Next.js 15 ships Tailwind v4 by default — verify `tailwindcss` is v4.x in package.json. Replace `app/page.tsx` with a placeholder that just renders `<main className="min-h-screen bg-base text-cream">website-v2 scaffold</main>` so Wave 2 can replace it cleanly.
  </action>
  <verify>
    <automated>cd website-v2 && pnpm install && pnpm exec next --version && test -f app/layout.tsx && test -f app/page.tsx</automated>
  </verify>
  <done>website-v2/ runs `pnpm install` cleanly; next.config.ts exists; framer-motion + shadcn deps installed</done>
</task>

<task type="auto">
  <name>Task 2: Port brand tokens + fonts + visual utilities into Tailwind v4 config and globals.css</name>
  <files>website-v2/tailwind.config.ts, website-v2/app/globals.css, website-v2/public/fonts/, website-v2/public/brand/</files>
  <action>
    Port brand tokens from brand/brand-voice.md §9 (per D-locked decisions in 13-CONTEXT.md) into website-v2/tailwind.config.ts:
      colors: { base: '#1A1A1A', 'base-alt': '#0F172A', surface: '#161616', 'surface-raised': '#1E1E1E', cream: '#F5F5F5', 'cream-dim': '#6B7280', purple: '#7B2FBE', 'purple-soft': '#9B4FDE', cyan: '#4DD9E8', orange: '#D97706', success: '#10B981', error: '#EF4444' }
      fontFamily: { display: ['"Roc Grotesk"','"Outfit"','system-ui','sans-serif'], body: ['"Gilroy"','"DM Sans"','system-ui','sans-serif'], mono: ['"JetBrains Mono"','monospace'] }
    Copy all brand/logo-*.{svg,png} and any webfonts referenced by brand-voice.md into website-v2/public/brand/ and website-v2/public/fonts/. Reuse website/favicon.svg → website-v2/app/favicon.ico (and /public/favicon.svg).
    In app/globals.css add:
      - @font-face declarations for Roc Grotesk and Gilroy pointing at /fonts/*
      - Base body styles: bg-base, text-cream, font-body, antialiased
      - `.dot-grid` utility: radial-gradient dot pattern at 8% opacity
      - `.arch-node` helper: small absolutely-positioned circle with purple→cyan glow (box-shadow + blur)
      - `.grain` overlay: fine-noise PNG or SVG filter at 4% opacity
      - `.gradient-brand` linear-gradient(135deg, #7B2FBE 0%, #9B4FDE 50%, #4DD9E8 100%)
    Do NOT invent new colors. Token hex values must match brand-voice.md §9 exactly.
  </action>
  <verify>
    <automated>cd website-v2 && grep -q '7B2FBE' tailwind.config.ts && grep -q '4DD9E8' tailwind.config.ts && grep -q 'Roc Grotesk' app/globals.css && grep -q 'dot-grid' app/globals.css && test -d public/brand</automated>
  </verify>
  <done>Tailwind config has locked palette + fonts; globals.css has dot-grid/arch-node/grain/gradient-brand utilities; brand assets in public/</done>
</task>

<task type="auto">
  <name>Task 3: Initialize shadcn/ui + install primitives + verify dev server + atomic commit</name>
  <files>website-v2/components.json, website-v2/components/ui/button.tsx, website-v2/components/ui/card.tsx, website-v2/components/ui/accordion.tsx, website-v2/components/ui/dialog.tsx, website-v2/lib/utils.ts</files>
  <action>
    From website-v2/ run: `pnpm dlx shadcn@latest init -d` (defaults: TypeScript, Tailwind, app dir, RSC, base color neutral, css var true). Then `pnpm dlx shadcn@latest add button card accordion dialog`. Confirm components.json was created and four primitives landed in components/ui/. Confirm lib/utils.ts `cn()` helper exists. Start dev server in background: `pnpm dev &` — wait 3s — `curl -sf http://localhost:3000 >/dev/null` — kill the dev server. Finally, from repo root, stage and commit atomically:
      git add website-v2 && git commit -m "scaffold(13-02): next.js 15 + tailwind v4 + shadcn/ui + brand tokens"
  </action>
  <verify>
    <automated>cd website-v2 && test -f components.json && test -f components/ui/button.tsx && test -f components/ui/card.tsx && test -f components/ui/accordion.tsx && test -f components/ui/dialog.tsx && test -f lib/utils.ts && git -C .. log -1 --oneline | grep -q '13-02'</automated>
  </verify>
  <done>shadcn initialized, 4 primitives present, dev server served 200 on /, atomic commit landed with `13-02` in message</done>
</task>

</tasks>

<success_criteria>
website-v2/ is a runnable Next.js 15 app. All brand tokens and visual utilities are wired. Wave 2 builders can `cd website-v2 && pnpm dev` and immediately start adding sections without touching config.
</success_criteria>

<output>
Create `.planning/phases/13-landing-page-rebuild-sameerautomations/13-02-SUMMARY.md` on completion.
</output>
</content>
