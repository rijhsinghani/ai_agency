---
phase: 13-landing-page-rebuild-sameerautomations
plan: 05
type: execute
wave: 3
depends_on: [13-03, 13-04]
files_modified:
  - website-v2/app/layout.tsx
  - website-v2/app/page.tsx
  - website-v2/app/api/lead/route.ts
  - website-v2/components/sections/lead-form.tsx
  - website-v2/app/sitemap.ts
  - website-v2/app/robots.ts
  - website-v2/public/favicon.svg
  - website-v2/Dockerfile
  - website-v2/.dockerignore
  - website-v2/next.config.ts
  - cloudbuild.yaml
  - .github/workflows/deploy-website.yml
autonomous: true
requirements: [LP-04, LP-05, LP-06]
must_haves:
  truths:
    - "Both wave2 builder worktrees are merged into main cleanly"
    - "Copy lint against brand/banned-phrases.json returns zero matches across website-v2/app and website-v2/components"
    - "Lead form POSTs to confirmed n8n webhook on hooks.rajphotovideo.com"
    - "Plausible analytics script loads in layout.tsx and fires 'book-call-click' event on CTA"
    - "OG/Twitter metadata in layout.tsx reuses website/index.html:7-28 copy"
    - "Dockerfile builds a Next.js standalone image that serves on :8080"
    - "Service sameer-automations-web is READY in Cloud Run us-east4"
    - "Cloudflare DNS points sameerautomations.com at the Cloud Run domain mapping"
  artifacts:
    - path: "website-v2/Dockerfile"
      provides: "Multi-stage Next.js standalone build"
    - path: "cloudbuild.yaml"
      provides: "gcloud build + deploy pipeline"
    - path: ".github/workflows/deploy-website.yml"
      provides: "CI deploy on push to main"
    - path: "website-v2/app/api/lead/route.ts"
      provides: "Server-side n8n webhook proxy"
  key_links:
    - from: "website-v2/components/sections/lead-form.tsx"
      to: "website-v2/app/api/lead/route.ts"
      via: "fetch POST"
    - from: "website-v2/app/api/lead/route.ts"
      to: "n8n webhook (hooks.rajphotovideo.com)"
      via: "server-side fetch with env N8N_LEAD_WEBHOOK_URL"
    - from: "Cloudflare DNS"
      to: "Cloud Run custom domain mapping"
      via: "CNAME ghs.googlehosted.com or A-record to Cloud Run LB"
---

<objective>
Wave 3 shipper plan. Merges the two Wave 2 builder branches, runs copy lint, wires forms to n8n, installs Plausible, adds SEO metadata + sitemap + robots, writes Dockerfile + cloudbuild.yaml + GitHub Action, deploys to Cloud Run us-east4, and maps the sameerautomations.com custom domain. Single-plan wave — the shipper is the main agent.

Purpose: Turn built-but-unwired code into a live production deployment at sameerautomations.com without cutting DNS yet (Wave 4 handles verification + cutover).

Output: `https://sameer-automations-web-<hash>-uc.a.run.app` serving the page, custom domain mapping created for sameerautomations.com, CI pipeline deployed on push.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/automation_consulting/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-CONTEXT.md
@~/.claude/plans/snazzy-sauteeing-pixel.md
@brand/banned-phrases.json
@website/index.html
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-03-SUMMARY.md
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Wave 2 worktrees + run copy lint + wire lead form to n8n</name>
  <files>website-v2/components/sections/lead-form.tsx, website-v2/app/api/lead/route.ts, website-v2/app/page.tsx</files>
  <action>
    From repo root main branch:
      git merge --no-ff wave2/builder-a -m "merge(13-05): wave2 builder-a sections"
      git merge --no-ff wave2/builder-b -m "merge(13-05): wave2 builder-b sections"
    Resolve any conflicts in app/page.tsx by composing the canonical section order from the PRD: Nav, Hero, IndustryProofStrip, ProblemStrip, AutomationCards, Proof, HowItWorks, FAQ, FinalCTA, Footer. Remove any stub exports builder-B created to fill missing builder-A files. Run `cd website-v2 && pnpm install && pnpm exec tsc --noEmit` — fix any type errors before proceeding.

    Copy lint gate — MUST pass with zero hits:
      node -e "const bp=require('./brand/banned-phrases.json'); const fs=require('fs'); const path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name); return e.isDirectory()?walk(p):[p]})}; const files=walk('website-v2/app').concat(walk('website-v2/components')).filter(f=>/\.(tsx?|jsx?|mdx?)$/.test(f)); let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log('HIT',f,':',p);hits++}}} if(hits){process.exit(1)}else{console.log('CLEAN')}"

    Confirm n8n webhook endpoint via n8n-mcp MCP. Look for an existing "lead-capture" or "website-lead" webhook on `hooks.rajphotovideo.com`. If none exists, create a minimal workflow that receives POST JSON `{name,email,business,industry,message,source:'sameerautomations.com'}` and forwards to Slack #claude. Record the full URL.

    Create website-v2/components/sections/lead-form.tsx:
      - shadcn Card with form: name, email, business name, industry (select — dental/salon/trades/cleaning/law/gym/real estate/tutoring/other), short message
      - Client component, useState for form + submitting + done flags
      - On submit: POST JSON to `/api/lead` — show success state on 200, inline error on fail
      - Peer-level placeholder copy. No emojis.

    Create website-v2/app/api/lead/route.ts:
      - Next.js route handler, POST only
      - Read N8N_LEAD_WEBHOOK_URL from process.env — fail fast with 500 if missing
      - Validate required fields (name, email, business). Reject malformed with 400
      - Forward JSON to n8n webhook with 10s timeout + try/catch. Return 200/4xx/5xx JSON
      - Include correlation id (crypto.randomUUID) in response and forwarded payload
      - Never expose internals in error response

    Add <LeadForm /> inside FinalCTA section (or as a sibling just above Footer — shipper picks whichever reads better).

    Commit atomically:
      git add website-v2 && git commit -m "feat(13-05): merge wave2 + lead form wired to n8n + copy lint clean"

  </action>
  <verify>
    <automated>cd website-v2 && pnpm exec tsc --noEmit && test -f components/sections/lead-form.tsx && test -f app/api/lead/route.ts && grep -q 'N8N_LEAD_WEBHOOK_URL' app/api/lead/route.ts && node -e "const bp=require('../brand/banned-phrases.json'); const fs=require('fs'); const path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name); return e.isDirectory()?walk(p):[p]})}; const files=walk('app').concat(walk('components')).filter(f=>/\.(tsx?|jsx?)$/.test(f)); let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log(f,p);hits++}}} process.exit(hits?1:0)"</automated>
  </verify>
  <done>wave2 branches merged, TypeScript clean, copy lint clean, lead form + n8n proxy route created</done>
</task>

<task type="auto">
  <name>Task 2: Wire Plausible analytics + SEO metadata + sitemap + robots + favicon</name>
  <files>website-v2/app/layout.tsx, website-v2/app/sitemap.ts, website-v2/app/robots.ts, website-v2/public/favicon.svg</files>
  <action>
    Update website-v2/app/layout.tsx:
      - Export `metadata` object. Reuse OG/Twitter copy verbatim from website/index.html:7-28:
        title: "Sameer Automations — Smart Systems. Built for Your Business."
        description: "I automated my own business. Now I build those same systems for other small business owners. Every lead gets a response in 60 seconds. Custom-built. Running in days."
        openGraph: { title, description, type: 'website', url: 'https://sameerautomations.com', siteName: 'Sameer Automations' }
        twitter: { card: 'summary_large_image', title, description }
        icons: { icon: '/favicon.svg' }
        metadataBase: new URL('https://sameerautomations.com')
      - Load self-hosted or cloud Plausible script in <head> using next/script strategy="afterInteractive":
          <Script defer data-domain="sameerautomations.com" src="https://plausible.io/js/script.tagged-events.js" />
      - Ensure lang="en", body classes load brand fonts.

    Copy website/favicon.svg → website-v2/public/favicon.svg (and /public/favicon.ico if a PNG source exists).

    Wire Plausible custom event on the two primary "Book a call" CTAs (hero + final-cta). Add `className="plausible-event-name=book-call-click"` per tagged-events convention. Add a data attribute for the CTA source (hero vs final) via `data-plausible-event-cta=hero`.

    Create website-v2/app/sitemap.ts (Next.js App Router MetadataRoute):
      export default function sitemap() { return [{ url: 'https://sameerautomations.com', lastModified: new Date(), changeFrequency: 'weekly', priority: 1 }] }

    Create website-v2/app/robots.ts:
      export default function robots() { return { rules: [{ userAgent: '*', allow: '/' }], sitemap: 'https://sameerautomations.com/sitemap.xml' } }

    Commit:
      git add website-v2/app/layout.tsx website-v2/app/sitemap.ts website-v2/app/robots.ts website-v2/public/favicon.svg website-v2/components && git commit -m "feat(13-05): SEO metadata + Plausible + sitemap + robots"

  </action>
  <verify>
    <automated>cd website-v2 && pnpm exec tsc --noEmit && grep -q 'plausible.io' app/layout.tsx && grep -q 'sameerautomations.com' app/layout.tsx && test -f app/sitemap.ts && test -f app/robots.ts && test -f public/favicon.svg && grep -q 'plausible-event-name=book-call-click' components/sections/hero.tsx</automated>
  </verify>
  <done>Metadata, Plausible script, tagged-event CTAs, sitemap, robots, favicon all wired</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfile + cloudbuild.yaml + GitHub Action + Cloud Run deploy + domain mapping</name>
  <files>website-v2/Dockerfile, website-v2/.dockerignore, website-v2/next.config.ts, cloudbuild.yaml, .github/workflows/deploy-website.yml</files>
  <action>
    Update website-v2/next.config.ts to enable standalone output:
      `const nextConfig = { output: 'standalone' }; export default nextConfig;`

    Create website-v2/Dockerfile (Next.js standalone multi-stage, PORT 8080):
      - Stage 1 `deps`: node:20-alpine, copy package.json + pnpm-lock.yaml, `corepack enable && pnpm install --frozen-lockfile`
      - Stage 2 `builder`: copy source + deps, run `pnpm build`
      - Stage 3 `runner`: node:20-alpine, NODE_ENV=production, PORT=8080, HOSTNAME=0.0.0.0, copy .next/standalone + .next/static + public, USER node, EXPOSE 8080, CMD ["node","server.js"]

    Create website-v2/.dockerignore: node_modules, .next, .git, research, *.md except none critical.

    Create cloudbuild.yaml at REPO ROOT (not inside website-v2) mirroring studio-os pattern:
      - Step 1: docker build from website-v2/ context → tag us-east4-docker.pkg.dev/$PROJECT_ID/sameer-automations/web:$SHORT_SHA
      - Step 2: docker push
      - Step 3: gcloud run deploy sameer-automations-web --image ... --region us-east4 --platform managed --allow-unauthenticated --port 8080 --cpu 1 --memory 512Mi --min-instances 0 --max-instances 5 --set-env-vars "N8N_LEAD_WEBHOOK_URL=${_N8N_LEAD_WEBHOOK_URL}"
      - Substitutions: _N8N_LEAD_WEBHOOK_URL (pull from Secret Manager in real deploy)
      - Options: logging=CLOUD_LOGGING_ONLY

    Create .github/workflows/deploy-website.yml (trigger: push to main, paths: website-v2/**). Authenticate via google-github-actions/auth@v2 using GCP_WIF_PROVIDER + GCP_SERVICE_ACCOUNT secrets (same pattern as studio-os — search ~/studio-os/.github/workflows/deploy-*.yml for the exact shape if uncertain). Run `gcloud builds submit --config cloudbuild.yaml --substitutions _N8N_LEAD_WEBHOOK_URL=...`

    Local verification before cloud deploy:
      cd website-v2 && docker build -t sameer-automations-web . && docker run -d -p 8080:8080 -e N8N_LEAD_WEBHOOK_URL=http://example.invalid --name saw-test sameer-automations-web && sleep 5 && curl -sf http://localhost:8080 >/dev/null && docker rm -f saw-test

    Cloud deploy — first-time manual submission (CI takes over after):
      export PROJECT_ID=$(gcloud config get-value project)
      # Ensure Artifact Registry repo exists
      gcloud artifacts repositories create sameer-automations --repository-format=docker --location=us-east4 --description="sameer automations images" 2>/dev/null || true
      gcloud builds submit --config cloudbuild.yaml --substitutions=_N8N_LEAD_WEBHOOK_URL="$(gcloud secrets versions access latest --secret=n8n-lead-webhook-url 2>/dev/null || echo 'https://hooks.rajphotovideo.com/webhook/placeholder')" .
      gcloud run services describe sameer-automations-web --region us-east4 --format='value(status.url)'

    Custom domain mapping (do NOT cut DNS yet — just create the mapping so the TLS cert provisions):
      gcloud beta run domain-mappings create --service sameer-automations-web --domain sameerautomations.com --region us-east4 2>/dev/null || gcloud beta run domain-mappings describe --domain sameerautomations.com --region us-east4
      # Capture the CNAME/A record it returns for Wave 4 to apply in Cloudflare

    Commit:
      git add website-v2/Dockerfile website-v2/.dockerignore website-v2/next.config.ts cloudbuild.yaml .github/workflows/deploy-website.yml && git commit -m "feat(13-05): dockerfile + cloudbuild + github action + cloud run deploy"

  </action>
  <verify>
    <automated>cd website-v2 && test -f Dockerfile && grep -q "output: 'standalone'" next.config.ts && cd .. && test -f cloudbuild.yaml && test -f .github/workflows/deploy-website.yml && gcloud run services describe sameer-automations-web --region us-east4 --format='value(status.conditions[0].status)' 2>&1 | grep -q True</automated>
  </verify>
  <done>Dockerfile builds locally, Cloud Run service sameer-automations-web is READY in us-east4, custom domain mapping created (awaiting DNS cut in Wave 4), CI workflow committed</done>
</task>

</tasks>

<success_criteria>
Deployed, TLS-provisioning Cloud Run service serving the full page over HTTPS at its \*.run.app URL. Lead form posts to n8n in production. Plausible loads and fires events. Custom domain mapping exists and is waiting for DNS. CI workflow can re-deploy on next push. Nothing is cut over yet — rollback is trivial.
</success_criteria>

<output>
Create `.planning/phases/13-landing-page-rebuild-sameerautomations/13-05-SUMMARY.md` on completion. Record: Cloud Run service URL, domain mapping DNS record to apply, n8n webhook URL used, any secrets added to Secret Manager.
</output>
