---
phase: 13-landing-page-rebuild-sameerautomations
plan: 04
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - website-v2/components/sections/automation-cards.tsx
  - website-v2/components/sections/proof.tsx
  - website-v2/components/sections/faq.tsx
  - website-v2/components/sections/final-cta.tsx
  - website-v2/components/sections/footer.tsx
  - website-v2/app/page.tsx
autonomous: true
requirements: [LP-02, LP-03, LP-04]
must_haves:
  truths:
    - "4 automation cards describe missed call text-back, instant lead response, review management, booking confirmations"
    - "3 proof blocks render one giant metric each from content/proof.json"
    - "FAQ accordion states setup price $1.5K–$3K + $200–500/mo retainer plainly"
    - "Final CTA shows dot-grid + arch-node visual with booking link + email"
    - "Footer shows contact, @SameerAutomates handle, tiny legal"
    - "All strings lint-clean against banned-phrases.json"
  artifacts:
    - path: "website-v2/components/sections/automation-cards.tsx"
      provides: "4 automation tile components"
    - path: "website-v2/components/sections/proof.tsx"
      provides: "3 case study metric blocks from proof.json"
    - path: "website-v2/components/sections/faq.tsx"
      provides: "shadcn Accordion with price transparency"
  key_links:
    - from: "website-v2/components/sections/proof.tsx"
      to: "website-v2/content/proof.json"
      via: "direct import of JSON at build time"
    - from: "website-v2/components/sections/final-cta.tsx"
      to: "Google Calendar booking URL + mailto:sameer@rajphotovideo.com"
      via: "anchor hrefs"
---

<objective>
Wave 2 builder-B plan. Build bottom-of-page sections (4 automation cards, 3 proof blocks, FAQ accordion, final CTA, footer) as production React components. Runs in isolated git worktree `~/.claude/worktrees/website-v2-builder-b` to parallelize with builder-A.

Purpose: Deliver the proof, depth, and conversion surface — the sections that close the sale after the hero establishes the promise.

Output: 5 section components wired into the page as HTML-comment placeholders (builder-A owns the composition order), each an atomic commit on branch `wave2/builder-b`.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/automation_consulting/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-landing-page-rebuild-sameerautomations/13-CONTEXT.md
@~/.claude/plans/snazzy-sauteeing-pixel.md
@website-v2/research/style-plan.md
@website-v2/content/proof.json
@brand/brand-voice.md
@brand/banned-phrases.json
@ops/packages/
</context>

<interfaces>
<!-- Contracts from 13-02 scaffold -->

From `website-v2/tailwind.config.ts`:

```ts
colors: { base, surface, cream, purple, 'purple-soft', cyan, orange }
fontFamily: { display: ['Roc Grotesk',...], body: ['Gilroy',...] }
```

From `website-v2/app/globals.css`: `.dot-grid`, `.arch-node`, `.grain`, `.gradient-brand`.

shadcn primitives at `@/components/ui/{button,card,accordion,dialog}`.

From `website-v2/content/proof.json` (produced by 13-01):

```ts
type ProofEntry = {
  industry: string;
  headline_metric: string; // e.g. "$4,200 recovered"
  timeframe: string;
  short_narrative: string;
  automation_type: string;
};
// File contains ≥3 entries across distinct non-photo industries
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Set up builder-B worktree + build Automation Cards + Proof sections</name>
  <files>website-v2/components/sections/automation-cards.tsx, website-v2/components/sections/proof.tsx</files>
  <action>
    Create isolated worktree from repo root:
      git worktree add ~/.claude/worktrees/website-v2-builder-b -b wave2/builder-b
    Then `cd ~/.claude/worktrees/website-v2-builder-b/website-v2 && pnpm install`.

    Build components/sections/automation-cards.tsx:
      - H2: "What gets built"
      - 4 shadcn Cards (md:grid-cols-2 lg:grid-cols-4), dark surface, purple→cyan gradient border on hover, lucide icon per card:
        1. Missed call text-back — icon MessageSquare. "A call comes in while you're on a job. The system texts them back in under 60 seconds with your availability. Most businesses recover 3–5 jobs a month from calls they would have lost."
        2. Instant lead response — icon Zap. "Form fills, DMs, inbound emails — all routed to one place and responded to in under 60 seconds with a real, personalized reply. You follow up when you can, but the lead never goes cold."
        3. Review management — icon Star. "Happy clients get asked for a review at the exact right moment, automatically. Reviews go up, ranking goes up, and you never have to ask anyone in person again."
        4. Booking confirmations — icon Calendar. "Every booking gets a confirmation, a reminder 24 hours out, and a follow-up after. No-shows drop. Clients show up on time and prepared."
      - Each card: 3 short lines (what it does / what it costs you without it / how fast it runs after install). Peer-level voice. Numerals for all metrics. No emojis.
      - Framer Motion: whileInView fade-up stagger

    Build components/sections/proof.tsx:
      - H2: "Real numbers"
      - Import content/proof.json directly: `import proof from '@/content/proof.json'`
      - Map entries → 3 large blocks, each with:
        * Giant headline metric (display font, 6–8xl, orange #D97706 or purple per style plan)
        * Industry tag (small, cream-dim)
        * Timeframe
        * 2-sentence narrative
        * automation_type badge at bottom
      - Alternate alignment left/right/left. Dot-grid and arch-node glows behind each metric.
      - Framer Motion: count-up animation on metric via whileInView

    Lint-check all strings against banned-phrases.json (zero matches). Commit atomically:
      git add website-v2/components/sections/automation-cards.tsx website-v2/components/sections/proof.tsx && git commit -m "feat(13-04): automation cards + proof blocks"

  </action>
  <verify>
    <automated>cd ~/.claude/worktrees/website-v2-builder-b/website-v2 && pnpm exec tsc --noEmit && grep -q 'Missed call text-back' components/sections/automation-cards.tsx && grep -q "proof.json" components/sections/proof.tsx && node -e "const bp=require('../../../../brand/banned-phrases.json'); const fs=require('fs'); const files=['components/sections/automation-cards.tsx','components/sections/proof.tsx']; let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log('HIT',f,p);hits++}}} process.exit(hits?1:0)"</automated>
  </verify>
  <done>4 automation cards + 3 proof blocks render from proof.json; TypeScript clean; lint clean; atomic commit landed</done>
</task>

<task type="auto">
  <name>Task 2: Build FAQ accordion + Final CTA + Footer and wire placeholders</name>
  <files>website-v2/components/sections/faq.tsx, website-v2/components/sections/final-cta.tsx, website-v2/components/sections/footer.tsx, website-v2/app/page.tsx</files>
  <action>
    Continue in `~/.claude/worktrees/website-v2-builder-b/website-v2`.

    Build components/sections/faq.tsx using shadcn Accordion:
      - H2: "Questions"
      - ≥6 items. Required topics + approved answers (paraphrase to brand voice, do not soften the price):
        1. "What does it cost?" — "Setup is $1,500–$3,000 for most automations. Retainer is $200–$500/mo for maintenance, monitoring, and tweaks. 50% upfront, 50% on completion. Retainer starts at go-live. That's the whole deal."
        2. "Why a retainer?" — "These systems need to stay running. APIs change, inboxes move, business rules evolve. The retainer covers active maintenance, not a subscription to software I already built."
        3. "What happens if I leave?" — "You keep the instance. I own the methodology. 30-day exit clause in the agreement. No lock-in, no hidden migration cost."
        4. "Who owns the automations?" — "You own your instance and your data. I own the patterns and templates I reuse across clients. Spelled out in plain English in the agreement."
        5. "How long does a build take?" — "Days, not weeks. Most first automations go live within 5–10 business days of scope sign-off."
        6. "Will you build an app?" — "No. I build discrete workflow automations — the kind that plug into tools you already use. If you need a full application, I'll tell you and point you somewhere else."
      - Sentence case. Oxford commas. Numerals. No emojis. No jargon.

    Build components/sections/final-cta.tsx:
      - Full-bleed section, dark base, dot-grid background, arch-node cluster on right side, gradient-brand accent bar top
      - H2: "Let's see if this fits."
      - Subhead: "15-minute discovery call. If automation won't help you, I'll say so."
      - Primary Button: "Book a call" → Google Calendar URL
      - Secondary ghost Button: "Email me" → mailto:sameer@rajphotovideo.com
      - Peer-level voice. No pressure.

    Build components/sections/footer.tsx:
      - Small dark footer, thin top border with gradient-brand
      - Left: logo + tagline "Smart systems. Built for your business. By Sameer."
      - Center: @SameerAutomates link (twitter/X, instagram, youtube — stub hrefs to `#` if unknown; shipper can finalize)
      - Right: © 2026 Sameer Automations. sameer@rajphotovideo.com
      - Tiny legal line: "Not affiliated with any past or current employer."

    Update app/page.tsx to import + compose all 5 sections. Place them after the builder-A placeholder comments:
      `<Nav /><Hero /><IndustryProofStrip /><ProblemStrip /><AutomationCards /><Proof /><HowItWorks /><FAQ /><FinalCTA /><Footer />`
      Note: builder-A owns nav/hero/strip/problem/how-it-works but may not have landed their commit yet in this worktree. Import ALL sections; if a builder-A file is missing, stub with a minimal export so TS compiles — shipper will resolve in Wave 3 merge.

    Lint-check every new string against banned-phrases.json. Commit atomically:
      git add website-v2/components/sections/faq.tsx website-v2/components/sections/final-cta.tsx website-v2/components/sections/footer.tsx website-v2/app/page.tsx && git commit -m "feat(13-04): faq + final cta + footer"

  </action>
  <verify>
    <automated>cd ~/.claude/worktrees/website-v2-builder-b/website-v2 && pnpm exec tsc --noEmit && grep -q '1,500' components/sections/faq.tsx && grep -q 'sameer@rajphotovideo.com' components/sections/final-cta.tsx && grep -q 'SameerAutomates' components/sections/footer.tsx && node -e "const bp=require('../../../../brand/banned-phrases.json'); const fs=require('fs'); const files=['components/sections/faq.tsx','components/sections/final-cta.tsx','components/sections/footer.tsx']; let hits=0; for(const f of files){const c=fs.readFileSync(f,'utf8').toLowerCase(); for(const p of (bp.phrases||bp)){if(typeof p==='string'&&c.includes(p.toLowerCase())){console.log('HIT',f,p);hits++}}} process.exit(hits?1:0)"</automated>
  </verify>
  <done>FAQ + final CTA + footer render with price transparency + brand voice; TypeScript clean; lint clean; atomic commit landed</done>
</task>

</tasks>

<success_criteria>
Builder-B's 5 sections are implemented, committed atomically on branch `wave2/builder-b`, TypeScript-clean, lint-clean. Ready for shipper to merge with builder-A's branch in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/13-landing-page-rebuild-sameerautomations/13-04-SUMMARY.md` on completion. Note worktree path and branch name for shipper merge.
</output>
