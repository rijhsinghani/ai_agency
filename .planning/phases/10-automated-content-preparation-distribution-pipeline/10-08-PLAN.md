---
phase: 10-automated-content-preparation-distribution-pipeline
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - content-engine/video-pipeline/main.py
  - content-engine/video-pipeline/services/content_generator.py
  - content-engine/video-pipeline/tests/test_content_generator.py
  - content-engine/n8n-workflows/slack-approval.json
autonomous: true
requirements: [SC-4, SC-5, SC-6]
gap_closure: true

must_haves:
  truths:
    - "Pipeline extracts content_bank_id from GCS object path and passes it through all steps"
    - "store_drafts_in_supabase is called after generate_platform_content succeeds, setting status=pending_review"
    - "Slack approval sets status=approved which independently triggers each platform-publisher workflow via their Supabase triggers"
  artifacts:
    - path: "content-engine/video-pipeline/main.py"
      provides: "content_bank_id extraction from GCS path + store_drafts_in_supabase call"
      contains: "store_drafts_in_supabase"
    - path: "content-engine/n8n-workflows/slack-approval.json"
      provides: "Publishing handoff documentation clarification"
      contains: "Execute Workflow"
  key_links:
    - from: "content-engine/video-pipeline/main.py"
      to: "content-engine/video-pipeline/services/content_generator.py"
      via: "store_drafts_in_supabase(content_bank_id, content, clip_paths, clip_metadata)"
      pattern: "store_drafts_in_supabase"
    - from: "content-engine/video-pipeline/services/content_generator.py"
      to: "Supabase content_bank table"
      via: "supabase.table('content_bank').update(status=pending_review)"
      pattern: "pending_review"
---

<objective>
Wire content_bank_id through the video pipeline and connect store_drafts_in_supabase + fix the Slack-to-publisher handoff.

Purpose: Close Gaps 1, 2, and 4 from 10-VERIFICATION.md. The pipeline currently generates content but never persists it to Supabase (store_drafts_in_supabase is imported but never called because content_bank_id is not available). The Slack approval handoff is a placeholder that needs clarification/documentation (the publisher workflows already trigger independently on status=approved via Supabase triggers, which is the correct architecture).

Output: main.py extracts content_bank_id from the GCS object path convention `raw/{content_bank_id}/video.mp4`, calls store_drafts_in_supabase after AI generation, and the Slack approval workflow notes are updated to document the independent-trigger publishing architecture.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-automated-content-preparation-distribution-pipeline/10-VERIFICATION.md
@.planning/phases/10-automated-content-preparation-distribution-pipeline/10-03-SUMMARY.md
@.planning/phases/10-automated-content-preparation-distribution-pipeline/10-04-SUMMARY.md

<interfaces>
From content-engine/video-pipeline/services/content_generator.py:
```python
def generate_platform_content(transcript: str, topic_title: str, hook: str, brand: str = "sameer_automations") -> dict
def store_drafts_in_supabase(content_bank_id: str, content: dict, clip_paths: list[str], clip_metadata: list[dict]) -> None
```

From content-engine/video-pipeline/config.py:

```python
GCS_RAW_PREFIX = "raw/"
GCS_OUTPUT_PREFIX = "output/"
GCS_TRIGGER_BUCKET = os.environ.get("GCS_TRIGGER_BUCKET", "video-pipeline-raw")
GCS_OUTPUT_BUCKET = os.environ.get("GCS_OUTPUT_BUCKET", "video-pipeline-output")
SUPABASE_URL = os.environ.get("SUPABASE_URL", "")
SUPABASE_SERVICE_KEY = os.environ.get("SUPABASE_SERVICE_KEY", "")
```

From content-engine/video-pipeline/main.py (current Step 11, lines 148-178):

```python
# Step 11: Content generation (Claude) — non-fatal
content = generate_platform_content(...)
# TODO: Pass content_bank_id from the trigger event metadata
# For now, log the generated content
logger.info(f"Generated YouTube title: {content.get('youtube_title')}")
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread content_bank_id through pipeline and wire store_drafts_in_supabase</name>
  <files>content-engine/video-pipeline/main.py, content-engine/video-pipeline/tests/test_content_generator.py</files>
  <behavior>
    - Test: GCS object name `raw/abc12345-def6-7890-ghij-klmnopqrstuv/my-video.mp4` extracts content_bank_id `abc12345-def6-7890-ghij-klmnopqrstuv`
    - Test: GCS object name `raw/my-video.mp4` (no UUID directory) results in content_bank_id=None and store_drafts_in_supabase is NOT called
    - Test: When content_bank_id is present and generate_platform_content succeeds, store_drafts_in_supabase is called with the correct arguments (content_bank_id, content dict, clip_paths list, clip_metadata list)
    - Test: When content_bank_id is None, store_drafts_in_supabase is NOT called and a warning is logged
    - Test: content_bank_id is also passed to trigger_thumbnail_generation (replacing the video_stem placeholder)
  </behavior>
  <action>
    1. In main.py `run_pipeline()`, after parsing `object_name`, extract `content_bank_id` from the GCS path. Convention: `raw/{content_bank_id}/filename.mp4` where content_bank_id is a UUID. Use a regex or path parsing: split object_name by `/`, if there are 3+ segments and segment[1] looks like a UUID (36 chars with hyphens), use it as content_bank_id. Otherwise content_bank_id = None.

    2. Add a helper function `_extract_content_bank_id(object_name: str) -> str | None` at module level that does this extraction. This makes it testable.

    3. In Step 11 (content generation), after `generate_platform_content()` succeeds:
       - If content_bank_id is not None: call `store_drafts_in_supabase(content_bank_id, content, clip_paths, clip_metadata)`
       - If content_bank_id is None: `logger.warning("No content_bank_id extracted from GCS path — skipping Supabase draft storage. Upload videos to raw/{content_bank_id}/filename.mp4 to enable automatic storage.")`
       - Remove the TODO comment and the logging-only placeholder code.

    4. In Step 12 (thumbnail trigger), pass the real content_bank_id instead of video_stem: `content_bank_id=content_bank_id or video_stem`

    5. Update tests in test_content_generator.py:
       - Add tests for `_extract_content_bank_id` with UUID path, non-UUID path, no subdirectory
       - Add integration test that mocks generate_platform_content and verifies store_drafts_in_supabase is called in the pipeline flow
       - Add test that when content_bank_id is None, store_drafts_in_supabase is NOT called

    The GCS path convention `raw/{content_bank_id}/video.mp4` is the simplest approach — no metadata lookups, no new database queries. The content_bank_id is baked into the upload path. Document this convention in a log message at pipeline start.

  </action>
  <verify>
    <automated>cd content-engine/video-pipeline && python -m pytest tests/test_content_generator.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>_extract_content_bank_id correctly parses UUID from GCS path. store_drafts_in_supabase is called when content_bank_id exists. Pipeline logs warning when no content_bank_id found. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Slack approval workflow publishing handoff</name>
  <files>content-engine/n8n-workflows/slack-approval.json</files>
  <action>
    The platform-publisher workflows (youtube.json, twitter.json, instagram.json, substack.json, threads.json) each have their own Supabase trigger watching for `status=approved`. This means when Slack approval sets `status=approved`, ALL publisher workflows fire independently via their triggers. This IS the correct architecture — it's event-driven, each publisher acts independently, and there's no single point of failure.

    Update the slack-approval.json to document this:

    1. Replace the "Flow 2: Handoff to Publishing Workflow" Set node's notes from the placeholder text to: "Publishing Architecture: When this workflow sets status=approved in Supabase, each platform-publisher workflow (youtube.json, twitter.json, instagram.json, substack.json, threads.json) independently detects the status change via their own Supabase triggers and fires within seconds. No Execute Workflow connection is needed — the event-driven trigger pattern provides independent, fault-tolerant publishing. Each publisher can fail without blocking others."

    2. Update the setup guide notes (item 9 "PUBLISHING HANDOFF") to replace the placeholder text with: "9. PUBLISHING HANDOFF\n   When Approve All is clicked, this workflow sets content_bank.status='approved' in Supabase.\n   Each platform-publisher workflow has its own Supabase trigger that fires on status=approved.\n   No manual wiring between this workflow and publishers is needed.\n   Ensure all platform-publisher workflows are imported and activated in n8n for end-to-end publishing."

    3. Keep the Set node itself as-is (it stores content_bank_id and approved_at for audit) — just update the notes to reflect the actual architecture instead of saying "connect to Execute Workflow later."

  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('content-engine/n8n-workflows/slack-approval.json')); handoff=[n for n in d['nodes'] if 'Handoff' in n['name']][0]; assert 'event-driven' in handoff['notes'].lower() or 'independently' in handoff['notes'].lower(), f'Notes not updated: {handoff[\"notes\"][:100]}'; print('Handoff notes updated correctly')"</automated>
  </verify>
  <done>slack-approval.json handoff node notes document the independent-trigger publishing architecture. Setup guide updated to explain that no manual workflow wiring is needed. Placeholder language removed.</done>
</task>

</tasks>

<verification>
1. `_extract_content_bank_id("raw/abc12345-def6-7890-ghij-klmnopqrstuv/video.mp4")` returns the UUID
2. `_extract_content_bank_id("raw/video.mp4")` returns None
3. All existing tests in test_content_generator.py still pass
4. All existing tests in test_clip_extraction.py still pass
5. slack-approval.json is valid JSON with updated handoff notes
</verification>

<success_criteria>

- store_drafts_in_supabase is called in main.py when content_bank_id is available
- content_bank_id extraction from GCS path is tested
- Slack handoff node documents the event-driven publishing architecture
- All tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/10-automated-content-preparation-distribution-pipeline/10-08-SUMMARY.md`
</output>
