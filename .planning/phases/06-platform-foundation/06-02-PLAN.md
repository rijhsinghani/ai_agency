---
phase: 06-platform-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - brand/youtube/about-section.md
  - .planning/phases/06-platform-foundation/06-CTA-AUDIT.md
  - .planning/phases/06-platform-foundation/06-FUNNEL-TEST.md
autonomous: false
requirements:
  - PLAT-03
  - PLAT-04

must_haves:
  truths:
    - "Every platform (YouTube, Twitter, Instagram) shows the exact CTA 'Book a free 15-min discovery call' with the same landing page URL"
    - "YouTube about section has the real booking URL — no [PLACEHOLDER] text remains"
    - "The full funnel works on mobile: each platform bio/description link reaches the landing page, which reaches the booking page, which reaches calendar confirmation"
    - "Funnel test results are documented with per-step pass/fail for all 4 entry paths"
  artifacts:
    - path: "brand/youtube/about-section.md"
      provides: "Updated YouTube about section with real booking URL replacing placeholder"
      contains: "calendar.app.google/psycao3CrXjGnmk48"
    - path: ".planning/phases/06-platform-foundation/06-CTA-AUDIT.md"
      provides: "Cross-platform CTA consistency audit checklist"
    - path: ".planning/phases/06-platform-foundation/06-FUNNEL-TEST.md"
      provides: "Mobile funnel test results — pass/fail per step per path"
  key_links:
    - from: "Twitter bio Website field"
      to: "website/index.html (landing page)"
      via: "Direct URL click"
      pattern: "landing.*page.*URL"
    - from: "Instagram bio Links field"
      to: "website/index.html (landing page)"
      via: "Direct URL click"
      pattern: "landing.*page.*URL"
    - from: "website/index.html booking CTA"
      to: "calendar.app.google/psycao3CrXjGnmk48"
      via: "CTA button click"
      pattern: "calendar\\.app\\.google"
    - from: "brand/youtube/about-section.md"
      to: "calendar.app.google/psycao3CrXjGnmk48"
      via: "Real booking URL replacing placeholder"
      pattern: "calendar\\.app\\.google"
---

<objective>
Audit CTA consistency across all platforms, fix the YouTube about section placeholder, and verify the full mobile funnel end-to-end.

Purpose: PLAT-03 requires identical CTA wording and destination across YouTube, Twitter, and Instagram. PLAT-04 requires documented proof that the full funnel (content to booked call) works on mobile. This plan ensures the three platforms are unified and the funnel is verified before the phase is marked complete.

Output: Updated about-section.md (real URL), CTA audit checklist, and mobile funnel test results document.
</objective>

<execution_context>
@/Users/sameerrijhsinghani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sameerrijhsinghani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-platform-foundation/06-CONTEXT.md
@.planning/phases/06-platform-foundation/06-RESEARCH.md
@.planning/phases/06-platform-foundation/06-01-SUMMARY.md

<interfaces>
<!-- Key data from research for executor reference -->

Confirmed booking URL: https://calendar.app.google/psycao3CrXjGnmk48
CTA wording (LOCKED — no variation): "Book a free 15-min discovery call"
Landing page: website/index.html (deployed URL is the CTA destination for all bio links)

YouTube about section file: brand/youtube/about-section.md

- Currently contains: https://calendar.google.com/calendar/appointments/schedules/[PLACEHOLDER]
- Must be replaced with: https://calendar.app.google/psycao3CrXjGnmk48
- This is the modern short-format URL (calendar.app.google) — correct format per research

Platform CTA locations:

- YouTube: About section description + video description links
- Twitter: Bio text + Website field (set up in Plan 01)
- Instagram: Bio text + Website/Links field (set up in Plan 01)

Mobile funnel test paths (LOCKED — all 4 must be tested):

1. YouTube video description link -> landing page -> booking page -> calendar confirmation
2. Twitter bio link -> landing page -> booking page -> calendar confirmation
3. Instagram bio link -> landing page -> booking page -> calendar confirmation
4. Pinned tweet link -> landing page -> booking page -> calendar confirmation

Pass criteria per step (LOCKED):

- Renders fully on mobile without horizontal scroll
- Tappable CTA button/link navigates correctly
- Reaches Google Calendar confirmation at final step
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update YouTube about section and create CTA audit checklist</name>
  <files>
    brand/youtube/about-section.md
    .planning/phases/06-platform-foundation/06-CTA-AUDIT.md
  </files>
  <action>
**Update brand/youtube/about-section.md:**
Read the current file. Replace the placeholder booking URL:
- OLD: `https://calendar.google.com/calendar/appointments/schedules/[PLACEHOLDER]`
- NEW: `https://calendar.app.google/psycao3CrXjGnmk48`
Also remove the HTML comment `<!-- Replace [PLACEHOLDER] with your actual Google Calendar appointment schedule ID -->`.
Keep all other content unchanged.

**Create 06-CTA-AUDIT.md:**
Create a cross-platform CTA consistency audit checklist. This is a document for the human to walk through on each platform and verify CTA match. Structure:

```markdown
# Cross-Platform CTA Audit — Phase 6

**Date:** [to be filled]
**Required CTA wording:** "Book a free 15-min discovery call"
**Required destination:** [landing page URL]
**Booking endpoint:** https://calendar.app.google/psycao3CrXjGnmk48

## YouTube

| Check                                   | Expected                              | Actual | Pass/Fail |
| --------------------------------------- | ------------------------------------- | ------ | --------- |
| About section contains CTA text         | "Book a free 15-min discovery call"   |        |           |
| About section booking URL               | calendar.app.google/psycao3CrXjGnmk48 |        |           |
| Video description template includes CTA | "Book a free 15-min discovery call"   |        |           |
| Video description links to landing page | [landing page URL]                    |        |           |

## Twitter (@SameerAutomates)

| Check                                          | Expected                            | Actual | Pass/Fail |
| ---------------------------------------------- | ----------------------------------- | ------ | --------- |
| Bio contains CTA text                          | "Book a free 15-min discovery call" |        |           |
| Website field links to landing page            | [landing page URL]                  |        |           |
| Pinned thread final tweet has CTA              | "Book a free 15-min discovery call" |        |           |
| Pinned thread final tweet has landing page URL | [landing page URL]                  |        |           |

## Instagram (@SameerAutomates)

| Check                                     | Expected                            | Actual | Pass/Fail |
| ----------------------------------------- | ----------------------------------- | ------ | --------- |
| Bio contains CTA text                     | "Book a free 15-min discovery call" |        |           |
| Website/Links field links to landing page | [landing page URL]                  |        |           |
| Post 3 (Offer) caption ends with CTA      | "Book a free 15-min discovery call" |        |           |
| Post 3 caption includes landing page URL  | [landing page URL]                  |        |           |

## Result

All checks passed: YES / NO
Date verified: [date]
```

The checklist must cover ALL three platforms. Every row checks for exact CTA wording match — no "Schedule a call" or "Learn more" variants allowed.
</action>
<verify>
<automated>grep -c "PLACEHOLDER" brand/youtube/about-section.md | grep "^0$" && grep -c "calendar.app.google/psycao3CrXjGnmk48" brand/youtube/about-section.md | grep -v "^0$" && test -f .planning/phases/06-platform-foundation/06-CTA-AUDIT.md && echo "PASS: about-section updated and CTA audit created" || echo "FAIL"</automated>
</verify>
<done> - brand/youtube/about-section.md has real booking URL (no placeholder) - 06-CTA-AUDIT.md exists with pass/fail rows for YouTube, Twitter, and Instagram - All CTA checks reference the exact wording "Book a free 15-min discovery call" - All URL checks reference the landing page as destination
</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile funnel test template</name>
  <files>.planning/phases/06-platform-foundation/06-FUNNEL-TEST.md</files>
  <action>
Create 06-FUNNEL-TEST.md following the structure from 06-RESEARCH.md Pattern 4 (Mobile Funnel Test Checklist Structure). This is the test template the human will fill in during verification.

Include all 4 funnel paths (LOCKED):

**Path 1: YouTube video -> description link -> landing page -> booking -> confirmation**
8 steps: Open YouTube video on mobile -> Tap description link -> Landing page loads (no horizontal scroll) -> Tap "Book a free 15-min discovery call" button -> Booking page loads (calendar.app.google) -> Calendar renders, time slots visible -> Select time slot, complete form -> Confirmation received

**Path 2: Twitter bio link -> landing page -> booking -> confirmation**
6 steps: Open Twitter @SameerAutomates profile -> Tap Website link in bio -> Landing page loads (no horizontal scroll) -> Tap booking CTA -> Booking page loads -> Calendar confirmation

**Path 3: Instagram bio link -> landing page -> booking -> confirmation**
6 steps: Open Instagram @SameerAutomates profile -> Tap Website link below bio -> Landing page loads (no horizontal scroll) -> Tap booking CTA -> Booking page loads -> Calendar confirmation

**Path 4: Pinned tweet link -> landing page -> booking -> confirmation**
6 steps: Open pinned tweet thread on Twitter -> Tap landing page URL in final tweet -> Landing page loads (no horizontal scroll) -> Tap booking CTA -> Booking page loads -> Calendar confirmation

Each path has a table with columns: Step | URL/Action | Pass/Fail | Notes

Include header fields for:

- Test date
- Real device model (e.g., iPhone 15, Pixel 8)
- Chrome DevTools emulation device (e.g., iPhone 14 Pro)
- Tester name

Include pass criteria per step (LOCKED from CONTEXT.md):

1. Renders fully on mobile without horizontal scroll
2. Tappable CTA button/link navigates correctly
3. Reaches Google Calendar confirmation at final step

Include a Result section at the bottom:

- All 4 paths: PASS / FAIL
- Blockers found (if any) — note: landing page fixes are deferred per CONTEXT.md, document but do not fix inline
- Date verified
  </action>
  <verify>
  <automated>test -f .planning/phases/06-platform-foundation/06-FUNNEL-TEST.md && grep -c "Path 1\|Path 2\|Path 3\|Path 4" .planning/phases/06-platform-foundation/06-FUNNEL-TEST.md | grep -v "^0$" && echo "PASS: Funnel test template created with all 4 paths" || echo "FAIL"</automated>
  </verify>
  <done> - 06-FUNNEL-TEST.md exists with all 4 funnel paths - Each path has step-by-step pass/fail table - Pass criteria match CONTEXT.md requirements (no horizontal scroll, tappable CTA, reaches confirmation) - Header fields for device info and test date - Result section with overall pass/fail and blocker documentation area
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human runs CTA audit and mobile funnel test</name>
  <what-built>
Two verification documents are ready:

1. **06-CTA-AUDIT.md** — Open each platform (YouTube Studio, Twitter profile, Instagram profile) and verify that every CTA says exactly "Book a free 15-min discovery call" and every link points to the landing page. Fill in the Actual and Pass/Fail columns.

2. **06-FUNNEL-TEST.md** — On your phone (real device) AND in Chrome DevTools mobile emulation, walk through all 4 funnel paths:
   - Path 1: YouTube video -> description link -> landing page -> booking -> confirmation
   - Path 2: Twitter bio link -> landing page -> booking -> confirmation
   - Path 3: Instagram bio link -> landing page -> booking -> confirmation
   - Path 4: Pinned tweet link -> landing page -> booking -> confirmation

For each step: does the page render without horizontal scroll? Is the CTA button tappable? Does it navigate to the correct destination?

Also: Open YouTube Studio -> Customization -> Basic info -> paste the updated about section from brand/youtube/about-section.md (now has real booking URL instead of placeholder). Save.
</what-built>
<how-to-verify>

1. Complete the CTA audit checklist (06-CTA-AUDIT.md) — all rows should show Pass
2. Complete the mobile funnel test (06-FUNNEL-TEST.md) — walk all 4 paths on real device
3. Update YouTube about section in YouTube Studio with the corrected copy from brand/youtube/about-section.md
4. Confirm: Does every path end at Google Calendar confirmation?
5. If any step fails: document it in the Notes column. If it is a landing page issue, note it as deferred per CONTEXT.md.

Type "funnel verified" if all 4 paths pass, or describe failures found.
</how-to-verify>
<resume-signal>Type "funnel verified" or describe failures</resume-signal>
</task>

</tasks>

<verification>
- brand/youtube/about-section.md contains real booking URL (no placeholder)
- 06-CTA-AUDIT.md has pass/fail for all 3 platforms
- 06-FUNNEL-TEST.md has pass/fail for all 4 funnel paths
- YouTube Studio about section updated with real booking URL
- All CTA wording matches exactly across all platforms
- All funnel paths reach Google Calendar confirmation on mobile
</verification>

<success_criteria>

1. Every platform (YouTube, Twitter, Instagram) uses identical CTA "Book a free 15-min discovery call" with the same landing page destination
2. YouTube about section no longer has [PLACEHOLDER] — real booking URL is live
3. All 4 mobile funnel paths are tested and documented with per-step pass/fail results
4. Any failures are documented (landing page issues deferred per CONTEXT.md)
   </success_criteria>

<output>
After completion, create `.planning/phases/06-platform-foundation/06-02-SUMMARY.md`
</output>
